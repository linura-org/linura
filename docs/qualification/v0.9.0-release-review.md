# v0.9.0 release-preparation review — First Boot and supported reference environment

**Status:** ready for protected release preparation; terminal publication/verification/closure evidence intentionally unrecorded  
**Claim class:** Experimental

This review is the final pre-publication cross-check for the `release: ready v0.9.0 — First Boot and supported reference environment` source. It freezes the semantic claim, security boundary and evidence requirements before the mechanical release controller changes version metadata or creates release authorization.

## Capability-boundary review

- [x] v0.9 remains Experimental;
- [x] the only platform-support claim is the exact `qualification/ubuntu-24.04-lts/amd64/qemu-tcg-headless` QualificationEnvironment;
- [x] the reference claim covers Linura installation/adoption on the pinned Ubuntu base image, not Ubuntu/bare-metal/disk/bootloader installation;
- [x] `agent_role = "proposal-only"` remains unchanged;
- [x] `managed_mutation_support = "narrow-experimental"` remains the inherited v0.6 narrow effect boundary;
- [x] First Boot is pre-authority and cannot mint policy decisions, approvals or executor authority;
- [x] interactive-owner, deferred-owner, unattended-local and recovery states are bounded/restart-safe internally, while the frozen user-facing v0.9 claim exposes only the production deferred-owner `--bootstrap` entry point;
- [x] exact Setup/MachineProfile selection is revalidated and deterministically bound to fresh authoritative observation before planning completes;
- [x] recovery is independently usable without First Boot/network/model dependencies.

## Security and authority review

- [x] durable bootstrap state is integrity checked, generation anchored and crash reconciled;
- [x] clone history is rejected through machine-id + DMI UUID binding;
- [x] effect verification is exact-bound and trusted-producer issued;
- [x] owner-enrollment and preparer-revocation evidence is authenticated, bound and freshness checked;
- [x] exact guest preparation uses the principal that is actually revoked before deferred owner handoff;
- [x] production First Boot can generate only narrow authenticated preparer-revocation evidence after re-observing fixed OS postconditions; final-owner signing remains unavailable to First Boot;
- [x] post-release closure consumes explicit roadmap `release_qualified_qualification_environments` metadata and advances `hardware/support-matrix.json` before machine-class validation;
- [x] `linura-firstboot --qualification-environment` reports candidate status before release preparation and `release-qualified-experimental` in the frozen 0.9.0 build;
- [x] Q8 validates the real nftables input chain, active/enabled/renamed SSH services and arbitrary-port SSH listeners, and insecure one-line/deb822 APT overrides;
- [x] qualification transport is isolated from product observation and is made durable before deliberate hard power cuts;
- [x] checkpoint restore, SQLite WAL recovery and interrupted package reconciliation are exercised rather than asserted by fixture labels.

## Qualification review

- [x] PR #129 exact reviewed head `c9509fb3a373fe8ac4ac5261c57e8b84b3c5ff78` passed CI;
- [x] exact-head Security passed;
- [x] exact-head CodeQL passed;
- [x] Disposable VM and Control1 plan-preview VM acceptance passed;
- [x] inherited v0.4 durability/ENOSPC, v0.5 executor/verifier, v0.6 managed lifecycle, v0.7 Library and v0.8 agent interpretation qualifications passed;
- [x] v0.9 QualificationEnvironment run `35131249048` passed exact-source contracts and the offline disposable VM;
- [x] all eight exact Ubuntu/QEMU-TCG adversarial shards passed;
- [x] canonical adversarial evidence assembly passed;
- [x] final v0.9 qualification evidence binding passed;
- [x] every PR #129 review thread was resolved before merge;
- [x] protected implementation merge `0c083490c858ec48c499013593369b4fa896f7f0` is the reviewed implementation lineage for this readiness boundary.

The dedicated v0.9 implementation qualification is exact-source evidence for readiness. Trusted Release Proof must still rerun it on the later exact release authorization; implementation evidence does not substitute for release proof.

## Frozen release-contract review

- [x] `docs/releases/v0.9.0.md` uses publication-stable wording and the canonical release-contract heading order;
- [x] the frozen contract names no supported PlatformProfile while explicitly bounding the supported QualificationEnvironment;
- [x] known limitations and explicit non-goals exclude bare metal, KVM, ARM64, workstation/desktop, arbitrary images and generic privileged mutation;
- [x] release wording does not widen model authority, executor scope or the v0.6 managed-effect namespace;
- [x] traceability binds PR #129 and its protected implementation merge;
- [x] roadmap release-contract metadata points to the frozen v0.9 contract while `current_release` remains v0.8.0 and `next_release` remains v0.9.0.

## Exact implementation evidence used for readiness

The final implementation head `c9509fb3a373fe8ac4ac5261c57e8b84b3c5ff78` passed all required PR gates. Its dedicated v0.9 run `35131249048` passed exact-source contract validation, offline disposable-system acceptance, all eight QEMU-TCG adversarial shards, canonical evidence assembly and final qualification binding. The protected squash merge is `0c083490c858ec48c499013593369b4fa896f7f0`.

Release-preparation changes now advance version/lockfile and freeze candidate-facing evidence without marking v0.9.0 published.

## Trusted Release Proof review

- [ ] metadata-only release authorization is tree-identical to the reviewed preparation source;
- [ ] protected-main CI/Security/CodeQL are fresh for the authorization source;
- [ ] Trusted Release Proof consumes dedicated v0.9 qualification evidence plus every inherited mandatory qualification;
- [ ] deterministic build, canonical digests, SBOM, proof receipt, provenance/attestations and independent binary reproduction are exact-bound.

## Publication review

- [ ] tag-last publication consumes the sealed payload without rebuilding it;
- [ ] immutable GitHub Release is bound to the exact authorized source;
- [ ] independent Release Verification redownloads and verifies published assets/evidence;
- [ ] protected post-release closure advances roadmap/support state only from verified terminal evidence;
- [ ] leased post-release cleanup removes only release-owned temporary refs and is idempotent.

Unchecked publication items remain intentionally pending until those artifacts actually exist.

## Decision

**Current decision: READY FOR RELEASE PREPARATION; PUBLICATION PENDING.**

Publication remains fail-closed behind the release-preparation exact-head gates, metadata-only authorization, Trusted Release Proof, sealed/reproduced construction, promotion, tag-last publication, independent verification, protected closure and leased cleanup.

This decision authorizes only the repository-defined protected release controller. It does not authorize a manual tag, manual asset rebuild, manual roadmap advancement or deletion of release-owned refs ahead of their lease checks.

## Terminal publication evidence

Terminal publication evidence is populated only by protected post-release closure after independent Release Verification succeeds.
