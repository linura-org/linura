# v0.7.0 release-preparation review

**Status:** ready for release preparation; publication pending.
**Claim class:** Experimental
**Supported platform profiles:** none

This review freezes the pre-publication cross-check for persistent intent lifecycle and the local Linura Library. Checked implementation items are not publication evidence; terminal status comes only from the repository's proof-first/tag-last release chain and independent published-release verification.

## Implementation and review closure

- [x] persistent intent lifecycle, revisions, optimistic concurrency and durable exact-operation idempotency are implemented;
- [x] append-only Setup/Profile revisions and deterministic current projections are implemented;
- [x] causal ownership/removal-impact analysis fails closed on incomplete evidence;
- [x] deterministic integrity-bound portable export/import/adoption is implemented without authority portability;
- [x] SQLite schema/version/migration/integrity/WAL/backup/restore behavior is implemented and bounded;
- [x] the v0.6 narrow managed-systemd authority boundary is unchanged;
- [x] corrective PR #94 closed every late PR #93 review finding and all #94 review findings;
- [x] final exact-head Codex review found no major issues;
- [x] corrected protected-main implementation source is `92d06961b5d7c86ce3cb6b3feddd02271a8c0a9a`;
- [x] fresh protected-main CI `34083271533`, Security `34083271504`, and CodeQL `34083271502` succeeded.

## Artifact and authority review

The v0.7 Library is an SDK/workspace capability, not a new privileged distributable. The sealed binary set remains exactly:

- `linurad`;
- `linuractl`;
- `linura-authorityd`;
- `linura-update-guard`;
- `linura-executor-systemd`.

`linura-firstboot` remains excluded. Imported/portable artifacts contain no approval, executor, transaction or secret authority. Adoption itself performs no external mutation.

## Historical pre-publication release checklist

- [ ] release-preparation PR exact-head CI/Security/CodeQL/v0.7 qualification succeed;
- [ ] release-preparation review completes with no unresolved findings;
- [ ] release-preparation tree is guarded squash-merged to protected `main`;
- [ ] a single-parent metadata-only release authorization is tree-identical to that reviewed merge and records exact `Reviewed-Source` / `Reviewed-Tree` trailers;
- [ ] release-authorization protected-main CI/Security/CodeQL succeed;
- [ ] Trusted Release Proof succeeds, including dedicated v0.7 qualification and all inherited required gates;
- [ ] sealed release binaries reproduce byte-for-byte and provenance/attestations verify;
- [ ] Release Promotion succeeds only after proof completion;
- [ ] immutable `v0.7.0` tag is created only during final publication;
- [ ] GitHub Release publishes the sealed bytes and frozen notes without rebuild;
- [ ] independent Release Verification redownloads and verifies the published release;
- [ ] protected post-release closure advances roadmap state only after terminal verification;
- [ ] obsolete temporary/release branches are deleted only after closure.

## Decision

**READY FOR RELEASE PREPARATION; PUBLICATION PENDING.**

No checklist item after release-preparation review may be inferred from prose or an earlier development run. Each must close from its actual exact-source release-control evidence.
