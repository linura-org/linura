# v0.8.0 release-preparation review — proposal-only agent interpretation

**Status:** released; terminal publication and independent verification complete.
**Terminal state note:** Requirement, decision and unchecked-checklist language explicitly labeled historical below preserves the pre-publication review snapshot; it is not current pending state.
**Claim class:** Experimental

This dossier records the final reviewed pre-publication cross-check for the `release: ready v0.8.0 — agent interpretation / IntentProposal` source. It records implementation and review readiness while deliberately withholding Trusted Release Proof, publication, independent-verification and terminal-closure claims until their protected lifecycle evidence exists.

## Capability-boundary review

Reviewed readiness criteria:

- [x] implementation matches `docs/milestones/v0.8.0.md` and ADR 0028;
- [x] `agent_role = "proposal-only"` remains exact;
- [x] no provider/model receives executor, policy-admin or implicit user authority;
- [x] Linura Control constructs/minimizes semantic context before it crosses into the agent runtime;
- [x] accepted proposals enter authoritative state only through the dedicated v0.8 Linura Control acceptance boundary backed by an authority-internal sealed Library acceptance primitive; generic public `LocalLibrary::create_intent` alone is insufficient;
- [x] external effects remain behind the inherited v0.6 authority lifecycle;
- [x] manual/no-provider operation remains complete;
- [x] platform support remains `none`.

## Proposal and provider review

- [x] `IntentProposal` schema/version/canonicalization is explicit and bounded;
- [x] assumptions, uncertainty and context/provider provenance are preserved where material;
- [x] raw secret-bearing Library/observation/retrieval/user context never crosses into the untrusted agent runtime;
- [x] provider adapters share one proposal contract and deterministic conformance fixtures;
- [x] Linura Control owns provider eligibility/discovery, selection, cross-adapter scheduling, aggregate deadlines/budgets, timeout/cancellation policy, retry/fallback/advisor admission, caching/coalescing and aggregation; the agent runtime can execute only a bounded Control-admitted attempt and cannot autonomously select/retry/fallback/reset budgets/aggregate providers;
- [x] untrusted runtime/adapter logic has no ambient provider egress, raw/reusable credentials or transport handles; a trusted invocation gate validates the exact credential-free prepared invocation and atomically consumes one unforgeable exact-bound Control permit before each provider call, and success/failure/cancellation/partial response spends it so adapter or transport-library retry/reissue/redirect/authentication replay/uncertain-write replay/reconnect/resume requires fresh Control admission against the original aggregate budget/deadline;
- [x] malformed/partial/oversized/unknown output fails closed;
- [x] provider timeout/cancel/rate-limit/unavailability cannot partially mutate authoritative intent;
- [x] offline mode rejects a registered/selected network-required adapter before adapter invocation or transport initialization, including discovery/health-check/fallback paths, with deterministic evidence that adapter and network trap counters remain zero;
- [x] prompt/tool injection cannot extend capabilities or lower approval requirements;
- [x] secrets/credentials are excluded from runtime/provider request, proposal and diagnostic surfaces.

## Trusted acceptance review

- [x] authenticated `Principal` is obtained independently from trusted transport/session state and proposal `Actor` remains provenance only;
- [x] Linura Control re-reads/re-resolves all authority-bearing observation, policy, Library, capability-registry and existing-intent dependencies at acceptance;
- [x] time-based observation freshness is evaluated using trusted Control time even when the observation revision is unchanged; expired evidence is reacquired or acceptance fails closed;
- [x] current authority-context binding is Control-owned and must exactly match the proposal binding;
- [x] capability references resolve only through the current Control-owned local capability registry;
- [x] the applicable human/policy acceptance decision is Control-owned and exact-bound to principal + proposal ID/digest + accepted context + operation identity + create/revise action + exact target identity/revision expectation;
- [x] the acceptance decision cannot be replayed over changed proposal/context/operation/target material;
- [x] the authority-internal durable acceptance transaction/write-CAS guard is acquired before final context/freshness/capability/decision revalidation;
- [x] every time-limited authority contributor is normalized under Linura Control to an **exclusive first-invalid instant** before aggregation; source endpoint semantics and clock units are preserved, and normalization failure/overflow fails closed rather than omitting a contributor;
- [x] the sealed capability carries the trusted-Control time floor captured during final revalidation plus the earliest normalized authority-validity deadline, and the authority-internal Library path permits durable write/CAS only when `time_floor <= now < deadline`; exact equality `now == deadline` and backward samples `now < time_floor` are invalid;
- [x] trusted Control time for one in-flight acceptance transaction is monotonic/fail-closed; post-mint clock rollback/reset or inability to prove continuity aborts with no durable mutation and cannot revive stale observation or expired decision/approval authority;
- [x] observation freshness is boundary-qualified separately: its own inclusive expiration endpoint remains current, but its normalized exclusive deadline is the first representable Control instant after that endpoint, and write/CAS exactly at the normalized deadline fails closed;
- [x] human/policy decision or approval expiry is boundary-qualified separately: when authority is invalid for `now >= expires_at`, that `expires_at` is the normalized exclusive deadline and write/CAS exactly at it fails closed;
- [x] the sealed acceptance-commit capability is transaction-scoped, exact-bound and non-user-constructible/non-deserializable;
- [x] the authority-internal Library persistence primitive consumes the capability without releasing/reacquiring the durable guard and mechanically re-checks the sealed trusted-time floor and normalized deadline at the actual intent+acceptance-record write/CAS linearization;
- [x] a database-lock, scheduling or internal-I/O delay that reaches or passes the sealed deadline because observation freshness expires aborts/rolls back with no durable intent or acceptance-record mutation;
- [x] the same post-mint delay with observation freshness still valid but the exact-bound acceptance decision/approval reaching its normalized exclusive deadline also aborts/rolls back with no durable mutation;
- [x] a post-mint backward trusted-Control time sample below the sealed floor aborts/rolls back with no durable mutation even when `now < deadline` would otherwise hold;
- [x] no public `LocalLibrary`/SDK method accepts caller-constructible proposal-acceptance authority material or can bypass the sealed Control capability;
- [x] direct public Library/SDK attempts to create proposal acceptance are rejected and leave durable state unchanged;
- [x] the authority-internal Library primitive atomically commits the intent transition and exact acceptance record;
- [x] the durable acceptance record retains decision identity/binding plus exact principal/proposal/context/operation/action/target, authority-generation/freshness evidence, decision/approval validity evidence, sealed trusted-time floor, normalized exclusive authority-validity deadline, endpoint-normalization evidence and resulting `IntentId`/revision;
- [x] exact retry after lost response/restart returns the same durable result and conflicting reuse fails closed;
- [x] a crash before durable commit cannot reconstruct/reuse the transient sealed capability and must re-enter Linura Control for fresh authority establishment;
- [x] acceptance creates only a normal `Intent` in `Proposed` state and grants no machine-mutation approval/executor authority.

## Qualification review

- [x] exact-head CI succeeds;
- [x] exact-head Security succeeds;
- [x] exact-head CodeQL succeeds;
- [x] dedicated v0.8 proposal/agent qualification succeeds, including Control-owned provider orchestration, negative runtime self-orchestration/budget-reset tests, production-equivalent adapter credential/direct-egress isolation, single-use permit forgery/replay/second-consumption rejection and adapter-internal retry-escape tests, Control-owned pre-runtime projection minimization, timer-only observation expiry, normalized exclusive authority-deadline derivation, exact-deadline cases for observation and decision/approval contributors, post-mint authority expiry at durable write/CAS linearization, post-mint authority-clock rollback rejection, offline pre-invocation rejection of network-required adapters, exact-bound decision replay rejection, concurrent authority-drift serialization, sealed Library direct-call rejection, and atomic lost-response/restart idempotency;
- [x] inherited v0.6/v0.7 authority, durability, Library and recovery qualification remains green;
- [x] every review finding is addressed and every review thread is resolved on the exact candidate head.

## Frozen release-contract review — historical pre-publication checklist

- [x] `docs/releases/v0.8.0.md` matches the implemented dedicated acceptance and proposal-only claim boundary;
- [x] release wording is publication-stable and contains no transient current-state assertions;
- [x] distributable artifact claims remain exact;
- [x] known limitations and non-goals remain explicit;
- [x] release contract does not widen platform support, executor scope or agent authority.

## Exact implementation evidence used for readiness

PR #103 final head `04b42c02096ed56ec7d93f22fbee47f87b068b57` passed canonical CI, Security, CodeQL/GitHub Advanced Security, dedicated v0.8 qualification and every inherited mandatory durability/VM qualification with all review threads resolved before its tree-identical squash merge `7e75526a6943c792b78a8e22251c8e3c75c5c202`.

The release-controller recovery path was subsequently repaired in PR #115 and re-qualified through the tree-identical readiness boundary merged by PR #116 at `c74c1d4098274cc0c313927b37c1286baf37f854`. Live Release Preparation run `34624066315` then proved the dedicated ASTuPDT GitHub App credentials, repository-scoped token minting, App authority, and exact-main CI/Security/CodeQL gates, while exposing one remaining recovery defect: the reviewed workspace was already versioned `0.8.0`, but Release Preparation only admitted the fresh two-file version-bump path. PR #117 is the fail-closed controller repair for that state and must itself pass exact-head native gates and protected merge before a fresh readiness boundary is established.

The machine roadmap intentionally remains `current_release = "v0.7.0"` / `next_release = "v0.8.0"`. Cargo version metadata is already `0.8.0` from an earlier deterministic preparation attempt. The repaired Release Preparation path must therefore re-prove workspace/lock coherence and create a SHA-addressed **zero-diff** preparation handoff rather than mutate release metadata a second time. Trusted Release Proof, tag-last publication, independent verification and terminal roadmap advancement remain unchecked below until their actual protected lifecycle evidence exists.

## Trusted Release Proof review — historical pre-publication checklist

- [ ] metadata-only authorization is tree-identical to the reviewed preparation source;
- [ ] protected-main CI/Security/CodeQL are fresh for the authorization source;
- [ ] Trusted Release Proof includes dedicated v0.8 qualification and inherited mandatory release gates;
- [ ] deterministic build, digests, SBOM, proof receipt, attestations and independent reproduction remain exact-bound.

## Publication review — historical pre-publication checklist

- [ ] tag-last publication uses the sealed payload without rebuild;
- [ ] immutable GitHub Release is bound to the exact authorized source;
- [ ] independent Release Verification redownloads and verifies published assets/evidence;
- [ ] protected post-release closure is reviewed before roadmap advancement;
- [ ] terminal current-release indexes/qualification state are synchronized by closure.

## Historical pre-publication decision

A release-readiness commit is permitted only after every pre-publication checklist item applicable at that stage has exact evidence. Terminal publication/verification/closure items remain controlled by the automated release lifecycle and must never be marked complete merely to make a candidate appear ready.

## Terminal publication evidence

Linura v0.8.0 completed the repository-defined protected proof-first, tag-last release lifecycle on 2026-09-11.

- Metadata-only release authorization: https://github.com/linura-org/linura/commit/0b6ddc862078d5ca1b5f648f0800a6d8abc0f593.
- Trusted Release Proof: GitHub Actions run `34634446617` — success.
- Release Promotion: run `34635918981` — success.
- Release publication: run `34635961722` — success.
- Immutable GitHub Release: `v0.8.0`, release id `387268125`, published `2026-09-11T18:56:55Z`.
- Independent Release Verification: run `34636145172` — success.

The immutable tag resolves to the exact source commit https://github.com/linura-org/linura/commit/0b6ddc862078d5ca1b5f648f0800a6d8abc0f593. Independent verification checked out the frozen release tag and verified published digests, canonical release metadata, GitHub Release immutability/attestation, and build provenance for every published candidate asset.

Post-publication repository bookkeeping advances the canonical roadmap to `current_release = "v0.8.0"` and `next_release = "v0.9.0"`. The frozen release contract and immutable GitHub Release remain unchanged.
